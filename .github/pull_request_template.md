## Summary

<!-- What changed and why -->

## Checklist

- [ ] Tested in [tagmanager.google.com](https://tagmanager.google.com) **Preview** mode with this `template.tpl`
- [ ] Re-tested after the most recent `pnpm build` (if `libs/` or `template.tpl` changed since the first test)
- [ ] Ran `pnpm lint`, `pnpm build`, and `pnpm test` locally (or CI is green)
- [ ] Attached screenshots or a short screen recording of Preview behavior (if UI-visible behavior changed)
